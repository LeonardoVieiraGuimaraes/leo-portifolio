version: "3.9"

networks:
  portifolio-network:
    driver: bridge

services:
  portifolio-professor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portifolio-professor
    restart: unless-stopped
    ports:
      - "8086:80"  # professor.leoproti.com.br
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - portifolio-network
    environment:
      - TZ=America/Sao_Paulo
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.portifolio-professor.rule=Host(`professor.leoproti.com.br`)"
      - "traefik.http.routers.portifolio-professor.tls=true"
      - "traefik.http.services.portifolio-professor.loadbalancer.server.port=80"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s